import React from "react";

const AboutPage = () => (
  <div className="about">
    <h2>About</h2>
    <p>
      This app can be used to create edit search and delete a book record from
      the library.
    </p>
  </div>
);

export default AboutPage;
